#ifndef MYCACHE_H
#define MYCACHE_H

#include <bits/c++config.h>

//typedef std::size_t size_t;

class MyCache
{
    public:
        MyCache();
        virtual ~MyCache();
//       ssize_t t(const &a) {
//            return a;
//        }
    protected:

    private:
};

#endif // MYCACHE_H
